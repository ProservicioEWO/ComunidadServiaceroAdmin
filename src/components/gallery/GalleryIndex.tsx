import { Card, CardBody, Text } from '@chakra-ui/react'

const GalleryIndex = () => {
    return (
        <Card w="full">
            <CardBody>
                <Text fontSize='lg'>Selecciona un evento para administrar la galeria de imágenes</Text>
            </CardBody>
        </Card>
    )
}

export default GalleryIndex